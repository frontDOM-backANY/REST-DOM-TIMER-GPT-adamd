const express = require('express');
// const db = require('../db_models/itnryModel.js')
// const { createError } = require('./../serverConfigs/globalErrorHandler.js')


const apiController = {
  // createErr: createError('apiController')
}

apiController.delete = async (req, res, next) => {

  return next();
}

apiController.getAll = async (req, res, next) => {

  return next();
}

apiController.getOne = async (req, res, next) => {

  return next();
}

apiController.patch = async (req, res, next) => {

  return next();
}

apiController.add = async (req, res, next) => {

  return next();
}

module.exports = apiController;